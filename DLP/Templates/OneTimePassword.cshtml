@model DLP.Application.Common.Templates.Models.OTPViewModel;
@using DLP.HtmlHelpers;

<!DOCTYPE html>
<html>
<head>
    <meta charset='utf-8'>
    <title>
        Your account is ready
    </title>
</head>
<body>
    <div style='max-width: 500px; margin: 0 auto; background-color: #f2f2f2; padding: 20px;'>
        <h1 style='color: #333333; text-align: center;'>
            <span>@await Html.Translate("one-time-password:email.title", "One-Time Password", Model.UserLang)</span>
        </h1>
        <p>
            <span>@await Html.Translate("one-time-password:entry.text", "Dear", Model.UserLang) @Model.RecipientName,</span>
        </p>
        <p>
            <span>@await Html.Translate("one-time-password:email.header", "Your One-Time Password (OTP) is", Model.UserLang):</span>
        </p>
        <div style='background-color: #ffffff; border: 1px solid #cccccc; padding: 10px; font-size: 18px; text-align: center; margin: 20px 0;'>
            @Model.OTPCode
        </div>
        <p>
            <span>
                @await Html.Translate("one-time-password:use.otp.code", "Please use this OTP to complete your action or verify your account. You can access your account by clicking this link: ", Model.UserLang)
                <a href="@Model.SiteUrl">@Model.SiteUrl</a>.
            </span>

        </p>
        <p>
            <span>@await Html.Translate("one-time-password:ignore.email", "If you didn't request this OTP, please ignore this email.", Model.UserLang)</span>
        </p>
        <p style='text-align: center; color: #888888; margin-top: 20px;'>
            <span>@await Html.Translate("one-time-password:greetings", "Thank you", Model.UserLang)</span>
        </p>
    </div>
</body>
</html>
